{
  "name": "socket-io-chat-app",
  "version": "1.0.0",
  "description": "Real-time chat application with Socket.io",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "echo '🚀 Starting build process...' && npm install && echo '📦 Installing server dependencies...' && cd server && npm install && echo '📦 Installing client dependencies...' && cd ../client && npm install && echo '🔨 Building client...' && npm run build && echo '✅ Build completed successfully!'",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 