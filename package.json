{
  "name": "socket-io-chat-app",
  "version": "1.0.0",
  "description": "Socket.IO Chat Application",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install --production=false && npm run build && cd .. && rm -rf public && mkdir -p public && cp -r client/dist/* public/ && ls -la public/",
    "dev": "cd client && npm run dev"
  },
  "keywords": ["socket.io", "chat", "real-time"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  }
} 