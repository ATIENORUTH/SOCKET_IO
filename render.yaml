services:
  - type: web
    name: socket-io-chat
    env: node
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_URL
        value: https://socket-io-2-aowx.onrender.com 